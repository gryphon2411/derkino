apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "derkino-infrastructure.fullname" . }}-test-connection"
  labels:
    {{- include "derkino-infrastructure.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "derkino-infrastructure.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
