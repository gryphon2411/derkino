apiVersion: v1
kind: Service
metadata:
  name: {{ include "derkino-infrastructure.fullname" . }}
  labels:
    {{- include "derkino-infrastructure.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "derkino-infrastructure.selectorLabels" . | nindent 4 }}
